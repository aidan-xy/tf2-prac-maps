TF2 competitve sixes maps that use VScript to randomly select and set up a certain "game state" (defined by point ownership and uber levels). Allows teams to practice different game states in a rapid-fire manner.

Requires convar **sv_allow_point_servercommand always**.
