TF2 competitve sixes maps that use VScript to allow you to setup a certain "game state" (defined by point ownership and uber levels). Allows teams to practice different game states in a more direct manner.

Requires convar **sv_allow_point_servercommand always**.
